---
/**
 * Registration Page - Bisa Bugar
 * Registration form with classic captcha (math-based)
 */

import "../styles/global.css";
import Header from "../components/layout/Header.svelte";
import Footer from "../components/layout/Footer.svelte";
import RegisterForm from "../components/auth/RegisterForm.svelte";
---

<!doctype html>
<html lang="id">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <meta
            name="description"
            content="Daftar akun Bisa Bugar dan mulai perjalanan fitness Anda dengan panduan profesional."
        />
        <title>Daftar - Bisa Bugar</title>
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    </head>
    <body id="body">
        <Header client:only="svelte" />

        <main class="auth-page">
            <div class="auth-container">
                <div class="auth-card">
                    <div class="auth-header">
                        <div class="logo-icon">BB</div>
                        <h1>Buat Akun Baru</h1>
                        <p>Bergabung dengan komunitas Bisa Bugar</p>
                    </div>

                    <RegisterForm client:only="svelte" />

                    <div class="auth-footer">
                        <p>
                            Sudah punya akun? <a href="/login">Login di sini</a>
                        </p>
                    </div>
                </div>
            </div>
        </main>

        <Footer />
    </body>
</html>

<style>
    .auth-page {
        min-height: calc(100vh - 80px);
        min-height: calc(100dvh - 80px);
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 1.5rem 1rem;
        background: linear-gradient(135deg, #fff7ed 0%, #fef3c7 100%);
        width: 100%;
        overflow-x: hidden;
    }

    @media (max-width: 480px) {
        .auth-page {
            padding: 1rem 0.75rem;
            align-items: flex-start;
            padding-top: 1rem;
        }
    }

    :global(.dark-mode) .auth-page {
        background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
    }

    .auth-container {
        width: 100%;
        max-width: 28rem;
        margin: 0 auto;
    }

    @media (max-width: 480px) {
        .auth-container {
            max-width: 100%;
        }
    }

    .auth-card {
        background: var(--color-surface);
        border-radius: 1.5rem;
        padding: 1.25rem;
        box-shadow: var(--shadow-2xl);
        border: 1px solid var(--color-border-light);
        width: 100%;
    }

    @media (min-width: 481px) {
        .auth-card {
            padding: 1.75rem;
            border-radius: 2rem;
        }
    }

    @media (min-width: 768px) {
        .auth-card {
            padding: 2.25rem;
        }
    }

    @media (min-width: 1024px) {
        .auth-card {
            padding: 2.5rem;
        }
    }

    .auth-header {
        text-align: center;
        margin-bottom: 1.25rem;
    }

    @media (min-width: 768px) {
        .auth-header {
            margin-bottom: 1.75rem;
        }
    }

    .logo-icon {
        width: 3rem;
        height: 3rem;
        background: var(--color-primary);
        border-radius: 0.75rem;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-weight: 900;
        font-size: 1rem;
        margin: 0 auto 0.875rem;
        box-shadow: 0 8px 20px -4px rgba(234, 88, 12, 0.4);
    }

    @media (min-width: 768px) {
        .logo-icon {
            width: 4rem;
            height: 4rem;
            font-size: 1.25rem;
            margin-bottom: 1.5rem;
            border-radius: 1rem;
        }
    }

    .auth-header h1 {
        font-size: 1.375rem;
        font-weight: 800;
        margin-bottom: 0.25rem;
    }

    @media (min-width: 768px) {
        .auth-header h1 {
            font-size: 1.75rem;
            margin-bottom: 0.5rem;
        }
    }

    .auth-header p {
        color: var(--color-text-tertiary);
        font-size: 0.8125rem;
    }

    @media (min-width: 768px) {
        .auth-header p {
            font-size: 0.875rem;
        }
    }

    .auth-footer {
        margin-top: 1.25rem;
        text-align: center;
        padding-top: 1rem;
        border-top: 1px solid var(--color-border-light);
    }

    @media (min-width: 768px) {
        .auth-footer {
            margin-top: 1.75rem;
            padding-top: 1.5rem;
        }
    }

    .auth-footer p {
        color: var(--color-text-tertiary);
        font-size: 0.8125rem;
    }

    @media (min-width: 768px) {
        .auth-footer p {
            font-size: 0.875rem;
        }
    }

    .auth-footer a {
        color: var(--color-primary);
        font-weight: 700;
    }

    .auth-footer a:hover {
        text-decoration: underline;
    }
</style>
